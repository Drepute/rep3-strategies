// import { StrategyParamsType } from '../../../../types';
// import { arithmeticOperand, viewAdapter } from '../../../../adapters/contract';
// import fetch from 'cross-fetch';

// const getSmashAndRockTypeCounts = async (
//   userName: string,
//   rockType: string
// ) => {
//   const res = await fetch(`https://api.peonnft.com/user/${userName}`);
//   const data = await res.json();
//   console.log('api', data);
// };

// export async function strategy({ eoa, options }: StrategyParamsType) {
//   // const thresholdCount = await getSmashAndRockTypeCounts(
//   //   options.questType,
//   //   eoa[0],
//   //   options
//   // );
//   // return arithmeticOperand(thresholdCount, options.threshold, options.operator);
// }
